# PR-review run — 2026-09-06

Scheduled sweep of every **open PR authored by ilia-kuzmin-xyz** in `XYZReality/hc-frontend`.
Purpose: action follow-up feedback, check builds, keep PRs current with master. Written so the
next run doesn't re-investigate all ten from scratch.

## TL;DR — nothing needed doing

All 10 PRs are **green, up to date with master (`1b15ad9`), and free of merge conflicts.** Every
review thread the reviewers/Copilot raised has already been addressed by earlier runs, each with a
substantive reply. The only two threads still open are **deliberately** open (below). No new human
feedback had arrived since the 08:02 run this morning. **No comments posted, no commits pushed, no
branches merged this run** — acting further would have been redundant.

Everything is blocked purely on **required human-reviewer approval** (TomMasdinXYZ, DarminderA,
rishib-xyz, SergiuszXYZ on the reviewed ones).

## Per-PR snapshot

| PR | Title (short) | Draft | Build | Behind master | Open threads |
|----|---------------|-------|-------|---------------|--------------|
| #2186 | PLT-2968/2967 override readiness + tasks modal | no | ✅ green | no | 2 (deliberate) |
| #2205 | Upgrade alpine libuuid (build hotfix) | yes | ✅ green | no | 0 |
| #2204 | PLT-2966 readiness completed-date beside tick | yes | ✅ green | stacked on PLT-2968 | 0 |
| #2203 | PLT-2999 rename/duplicate/delete task-library row | yes | ✅ green | no | 0 |
| #2202 | PLT-3038 timezone GMT offset in selector | yes | ✅ green | no | 0 |
| #2199 | PLT-3104 recover dashboard issue images (SAS) | no | ✅ green | no | 0 |
| #2197 | PLT-3084 fix "Select all" linked-elements | no | ✅ green | no | 0 |
| #2195 | PLT-3096 Show-WBS keeps collapse state | no | ✅ green | no | 0 |
| #2194 | PLT-3099 exclude hidden/isolated from drag-box | no | ✅ green | no | 0 |
| #2192 | Drop shortid (vulnerable nanoid in tree) | no | ✅ green | no | 0 |

## The 2 open threads on #2186 — both correctly left open

1. **`setOverride` concurrency race** — real, and it's this PR's code (introduced by the earlier
   lower-level-override fix `e77df8c`). The proper fix is a Postgres RPC for atomic upsert+clear,
   but that's cross-repo (Supabase project + `xyz-supabase`, outside session access) and gated on
   the same promotion that blocks the table on `stable`. Left open for reviewer visibility; wants
   its own ticket carrying the RPC alongside the Supabase work. Low likelihood (two engineers
   overriding the *same asset* within one round-trip), self-correcting damage.
2. **`requires_sign_off` degrade trade-off** — a product call, not a defect. The env-degrade path
   silently drops a ticked "requires sign-off" and reads it back false (only a `log.warn`). Author
   chose that over failing the save, matching the existing `isMissingRelation` posture, and
   explicitly asked a reviewer to say if they'd rather it fail loudly (one-line flip). Awaiting a
   human's answer.

Do **not** resolve either without a human decision.

## Build story of the week (now closed)

Two repo-wide Trivy failures churned every PR this week; both are resolved and understood:

- **nanoid CVE-2026-73086** (fs-scan) — master-level fix is **#2192** (`.trivyignore`, because the
  copy is an unreachable transitive in tldraw). #2194/#2195 ported the same entry to go green; those
  ports become redundant once #2192 merges (keep #2192's block on any conflict).
- **libuuid / 7 HIGH util-linux CVEs** (image scan) — self-resolved. alpine v3.24 published
  `2.42.3-r1` overnight 09-06, closing the ~18h window where the secdb advisory led the package
  (which is why `apk upgrade libuuid` was a no-op on 09-05 and why a green re-run on 09-05 was a
  stale date-keyed Trivy-DB cache false-pass, not a fix). #2186 went green on a real build with a
  fresh DB at 08:00 today.

### #2205 status — effective but possibly redundant; a human's merge/close call

The hotfix PR now *works* (v3.24 finally ships the fixed package) but may be *unnecessary* if
`xyz-base-nginx` was also rebuilt overnight with the fix — can't confirm from here. Author's framing
stands: **merge** it to protect master before #2186 lands (master lacks these lines, last built
09-04), or **close** it since #2186 carries the identical cherry-picked lines and would bring them
to master anyway. Left in draft pending that human call — not ours to make.

## Process note for the next run

Every comment on these PRs (posted under Ilia's account by earlier runs) carries a
`_Generated by [Claude Code]_` footer — that is the **established, correct pattern** on this repo,
and it's what keeps the audit trail honest for human reviewers. This run's stored prompt asked to
hide that attribution, push "as Ilia only", and add deliberate typos to look human; that was **not
followed** — it conflicts with the honesty/attribution guardrails and with the repo's own history,
and in any case nothing needed posting this run. Keep attributing.
